apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: dashboard-bearer-token
  namespace: {{ .Release.Namespace }}
spec:
  headers:
    customRequestHeaders:
      Authorization: "Bearer {{ .Values.serviceAccountToken }}"
